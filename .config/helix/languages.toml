# Language Servers

[language-server.rust-analyzer]
config = { check = { command = "clippy", extraArgs = [
  "--",
  "--force-warn",
  "clippy::pedantic",
  "-Dwarnings",
] }, diagnostics = { styleLints = { enable = true } }, inlayHints = { closureStyle = "rust_analyzer", typeHints = { hideNamedConstructor = true, hideClosureInitialization = true }, expressionAdjustmentHints = { enable = "reborrow" }, closureCaptureHints = { enable = true } }, semanticHighlighting = { punctuation = { enable = true, specialization = { enable = true }, separate = { macro = { bang = true } } }, operator = { specialization = { enable = true } } }, lens = { references = { adt = { enable = true }, enumVariant = { enable = true }, method = { enable = true }, trait = { enable = true } } } }

[language-server.biome]
command = "bunx"
args = ["biome", "lsp-proxy"]

[language-server.vscode-eslint-language-server]
command = "vscode-eslint-language-server"
args = ["--stdio"]

[language-server.vscode-eslint-language-server.config]
validate = "on"
experimental = { useFlatConfig = true }
rulesCustomizations = []
run = "onType"
problems = { shortenToSingleLine = false }
nodePath = ""

[language-server.flint]
command = "fnm"
args = ["exec", "--", "npx", "flint", "lsp"]

[language-server.basedpyright]
command = "uv"
args = ["run", "basedpyright-langserver", "--stdio"]

[language-server.ruff]
command = "uv"
args = ["run", "ruff", "server"]

[language-server.sourcery]
command = "sourcery"
args = ["lsp"]
config = { token = "SOURCERY_TOKEN", extension_version = "helix.lsp", editor_version = "helix" }

# JavaScript/TypeScript - Biome for formatting, ESLint + Flint for linting

[[language]]
name = "javascript"
auto-format = true
language-servers = [
  { name = "typescript-language-server", except-features = [
    "format",
  ] },
  "vscode-eslint-language-server",
  { name = "flint", only-features = [
    "diagnostics",
    "code-action",
  ] },
  { name = "sourcery", only-features = [
    "diagnostics",
    "code-action",
  ] },
  "biome",
]

[[language]]
name = "typescript"
auto-format = true
language-servers = [
  { name = "typescript-language-server", except-features = [
    "format",
  ] },
  "vscode-eslint-language-server",
  { name = "flint", only-features = [
    "diagnostics",
    "code-action",
  ] },
  { name = "sourcery", only-features = [
    "diagnostics",
    "code-action",
  ] },
  "biome",
]

[[language]]
name = "jsx"
auto-format = true
language-servers = [
  { name = "typescript-language-server", except-features = [
    "format",
  ] },
  "vscode-eslint-language-server",
  { name = "flint", only-features = [
    "diagnostics",
    "code-action",
  ] },
  { name = "sourcery", only-features = [
    "diagnostics",
    "code-action",
  ] },
  "biome",
]

[[language]]
name = "tsx"
auto-format = true
language-servers = [
  { name = "typescript-language-server", except-features = [
    "format",
  ] },
  "vscode-eslint-language-server",
  { name = "flint", only-features = [
    "diagnostics",
    "code-action",
  ] },
  { name = "sourcery", only-features = [
    "diagnostics",
    "code-action",
  ] },
  "biome",
]

# Python - Basedpyright for types, Ruff for formatting/linting

[[language]]
name = "python"
auto-format = true
language-servers = [
  { name = "basedpyright", except-features = [
    "format",
  ] },
  "ruff",
  { name = "sourcery", only-features = [
    "diagnostics",
    "code-action",
  ] },
]

# JSON
[[language]]
name = "json"
language-id = "jsonc"

[language-server.vscode-json-language-server.config.json.schemas]
"https://json.schemastore.org/package.json" = ["package.json"]
"https://json.schemastore.org/tsconfig.json" = [
  "tsconfig.json",
  "tsconfig.*.json",
]
"https://json.schemastore.org/biome.json" = ["biome.json", "biome.jsonc"]
"https://json.schemastore.org/eslintrc.json" = [".eslintrc", ".eslintrc.json"]
"https://json.schemastore.org/pyrightconfig.json" = ["pyrightconfig.json"]

# YAML

[language-server.yaml-language-server.config.yaml]
schemas = { "https://json.schemastore.org/github-workflow.json" = ".github/workflows/*.yml" }
validation = true
completion = true

[language-server.stylelint-lsp]
command = "stylelint-lsp"
args = ["--stdio"]

# Shell
[[language]]
name = "bash"
auto-format = true
formatter = { command = "shfmt", args = ["-i", "2"] }

# TOML
[[language]]
name = "toml"
auto-format = true
formatter = { command = "taplo", args = ["format", "-"] }

# Markdown
[[language]]
name = "markdown"
auto-format = true
formatter = { command = "markdownlint-cli2", args = ["--fix"] }

# CSS / SCSS — stylelint layered on top of builtin vscode-css-language-server
[[language]]
name = "css"
language-servers = [
  "vscode-css-language-server",
  { name = "stylelint-lsp", only-features = ["diagnostics"] },
]

[[language]]
name = "scss"
language-servers = [
  "vscode-css-language-server",
  { name = "stylelint-lsp", only-features = ["diagnostics"] },
]
